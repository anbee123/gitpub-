<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Welcome to Gitpub</title>
        <style type="text/css">
            body {
              color: rgb(238, 215, 229);
              background-color: rgb(174, 101, 171);
            }
          </style>
    </head>
    <body>
        <h1>Welcome to Gitpub</h1>
        
        <ul>
            <% for(let i = 0; i < allDrinks.length; i++){ %>
                <li>
                    <a href="/drinks/<%= i %>">
                        <%= allDrinks[i].name %>
                    </a>
                </li>
            <% } %>
        </ul>
        <h2>Food Menu</h2>
    
            <ul>
            <% for(let i = 0; i < allFood.length; i++){ %>
                <li>
                    <a href="/food/<%= i %>">
                        <%= allFood[i].name %>
                    </a>
                </li>
                </ul>
            <% } %>

    </body>
</html>
